/**
 * @param {number[]} nums
 * @return {number}
 */
var thirdMax = function(nums) {
    const len = nums.length
    if(len < 3){
        return Math.max(...nums)
    }
    var obj = {}
    for (let i = 0; i < len; i++) {
    }
};